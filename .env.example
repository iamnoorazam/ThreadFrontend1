# Vite dev server port
VITE_PORT=5173

# Base URL for API calls. With the Vite proxy, /api is forwarded to the
# backend automatically in dev. Set to the full backend URL when using
# a deployed server.
VITE_API_URL=/api
